import { TetrisScreen } from './TetrisScreen'

/**
 * The product has one mini-game — Завтрак-тетрис (Figma 133:1714).
 * Old play frames all open that screen.
 */
export function PlayScreen() {
  return <TetrisScreen />
}
